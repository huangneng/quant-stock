# 全历史成交额/成交量精度回标

- [✓] Task 1: 备份库并记录基线
    - 1.1: 备份 `kline.db` 到带时间戳的文件
    - 1.2: 记录总行数、交易日数、`amt_src` 分布作为基线
    - 1.3: 存档若干抽样行，供回标后对比

- [✓] Task 2: 新增 `scripts/backfill_amt_src.py`
    - 2.1: 参数 `--start/--end/--dry-run/--limit`，默认覆盖库内全区间
    - 2.2: baostock 登录校验 `error_code`，失败立即中止不写库
    - 2.3: 逐只票拉整区间 `date,amount,volume`，每 500 只重连
    - 2.4: 只更新库内已存在的行，`amount <= 0` 跳过
    - 2.5: 每 200 只提交一次，中断可重跑且幂等

- [✓] Task 3: 统计与报告
    - 3.1: 输出更新行数、跳过数、查不到数据的代码数
    - 3.2: 统计 amount 实质变化的行数与偏差分位
    - 3.3: 统计 volume 实质变化的行数（腾讯按手取整的影响面）

- [✓] Task 4: 为回标脚本补测试
    - 4.1: 用临时库验证只更新已有行、不新增行
    - 4.2: 验证 `amount <= 0` 被跳过且不标 exact
    - 4.3: mock baostock 登录失败，断言中止且未写库
    - 4.4: 验证 `--dry-run` 不写库
    - 4.5: 验证重跑幂等（第二次更新行数为 0）

- [✓] Task 5: 小样本试跑
    - 5.1: `--limit 50 --dry-run` 确认统计口径正确
    - 5.2: `--limit 50` 实跑，逐行核对这 50 只的 amount/volume 与 baostock 一致
    - 5.3: 确认这 50 只的 `amt_src` 变为 exact

- [✓] Task 6: 全量回标
    - 6.1: 全区间跑完 5207 只，记录耗时与统计
    - 6.2: 校验 `amt_src` 覆盖率与失败代码数
    - 6.3: 抽查若干历史日期的 amount 与 baostock 逐字段一致

- [✓] Task 7: 下游验证与提交
    - 7.1: 跑一次 `daily_select` 与报告生成，断言 exact 行未被改写
    - 7.2: 对比回标前后近期选股结果，记录变化
    - 7.3: 刷新总览页，提交并推送
    - 7.4: 生成 summary.md，记录偏差统计与历史结果可能变化的说明

- [✓] Task 8: 整理 `kline.db` 出版本控制的方案（只出方案，不执行）
    - 8.1: 统计 `.git` 体积构成与 kline.db 的历史版本数
    - 8.2: 列出可选路径（gitignore + 重建说明 / Git LFS / 历史重写）及各自代价
    - 8.3: 明确重写历史的风险与不可逆点，写入 summary 供决策
