# 半截 K 线修复任务计划

先做检测手段（Task 1），它是修复三的验收标准——没有它就无法证明存量修好了。
再改代码（Task 2、3），最后才动存量数据（Task 4），顺序不能倒：
必须先堵住写入口，否则修完又被盘中调用写脏。

Task 5 的重跑依赖 Task 4 的数据修复，放在最后。

- [✓] Task 1: 半截行检测
    - 1.1: `scripts/audit_kline_integrity.py` 新增 `--check-latest`
    - 1.2: 批量拉新浪快照（600 只/请求），以响应自带的日期字段为准，不假设「今天」
    - 1.3: 与库内该日期的 `volume` 比对，按偏差区间（<1% / 1~50% / 50~90% / >90%）统计
    - 1.4: 输出偏差最严重的若干只，并标注是否为历史选股标的
    - 1.5: 库内无该日期数据时明确报告，不静默通过
    - 1.6: 在当前库上跑一次，确认能复现 302 只偏差

- [✓] Task 2: 同步跳过规则改为 `last > end`
    - 2.1: `sync_kline_db` 把 `if last and last >= end` 改为 `last > end`，注释写明半截行成因
    - 2.2: 确认 `real_start` 计算在 `last == end` 时仍正确（应从 end 当天重取）
    - 2.3: 单测：库内已有 `date == end` 的行时，该票仍被重取且行被覆盖
    - 2.4: 单测：库内有超过 end 的行时仍然跳过
    - 2.5: 确认早停与失败率记账不受影响（`attempted` 口径变化）

- [✓] Task 3: 盘中不落库当日 K 线
    - 3.1: 新增模块级判定函数：给定日期，判断是否「当日且未到 15:00」
    - 3.2: `sync_kline_db` 落库前过滤掉该日期的行
    - 3.3: `get_kline` 的按需补拉落库前做同样过滤，但**返回值仍包含**该行
    - 3.4: 单测：模拟盘中（14:00）取到含当日的数据，断言库内无当日行、返回值有当日行
    - 3.5: 单测：模拟盘后（16:00），断言当日行正常落库
    - 3.6: 单测：回补历史日期不受守卫影响
    - 3.7: 单测：过滤后整个 DataFrame 为空时不调用 `upsert_kline`，不报错

- [✓] Task 4: 存量修复
    - 4.1: 备份 `stock_data/kline.db`
    - 4.2: 先用 `--check-latest` 记录修复前的偏差基线
    - 4.3: 对 08-21 ~ 08-28 窗口执行 `sync_kline_db(full=True)` 强制重取覆盖
    - 4.4: 过程中记录 `fail_rate` / `aborted` / 熔断统计；若被 WAF 中断则记录已修范围
    - 4.5: 重跑 `--check-latest`，确认偏差 >1% 的票数归零
    - 4.6: 抽样核对 `sh.600133`、`sh.600127` 的量与新浪一致
    - 4.7: 跑 `audit_kline_integrity.py`（无参），确认未引入手/股单位错误

- [✓] Task 5: 下游重跑与验证
    - 5.1: 重跑 `daily_select.py --date 2026-08-28`，对比修复前后的候选与评分
    - 5.2: 若候选集变化，明确说明变化原因（量能项与成交额项的输入变了）
    - 5.3: 刷新总览页
    - 5.4: 编译检查与导入检查通过，复跑既有四套单测
    - 5.5: 撰写 `summary.md`
