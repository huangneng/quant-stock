# 快照优先的补数通路 + 预筛兜底不再空转

- [x] Task 1: 摸清 `require_today` 分支对快照 `date` 的依赖
    - 1.1: 记录 `router.py:337-345` 现状——`new_row` 直接取 `row[k] for k in UNIFIED_COLS`，`date` 来自快照
    - 1.2: 确认现状缺陷：`:339` 只判断 `today_iso not in df['date']`，开盘前调用会把前一日收盘伪装成今日行
    - 1.3: 为改动前的行为写一个基线测试，锁住「快照日期 == 今天」时的既有正确路径
    - 1.4: 结论写回 doc.md 的影响面小节

- [x] Task 2: `sina.py` 解析行情自带日期
    - 2.1: 解析第 30 位为 `date`、第 31 位为 `snapshot_time`
    - 2.2: 第 30 位缺失或为空时跳过该行（不再回填 `pd.Timestamp.now()`）
    - 2.3: 删掉 `today_iso` 变量，注释写清为何不能用「今天」
    - 2.4: 保留 `turn = 0.0` 与既有的 `close/amount/prev_close <= 0` 过滤

- [x] Task 3: 修正 `require_today` 分支
    - 3.1: 仅当 `row['date'] == today_iso` 才拼接，否则丢弃并说明原因
    - 3.2: 拼接后按 `date` 去重，避免与库内同日行重复
    - 3.3: `snapshot_time` 不进 `UNIFIED_COLS`，确认 `new_row` 取值不被污染

- [x] Task 4: 为快照日期语义补测试
    - 4.1: mock 快照返回真实日期，断言 `date` 为行情日期而非今天
    - 4.2: 构造「开盘前调用」——快照日期为前一交易日，断言 `require_today` 不拼接
    - 4.3: 构造退市股场景——快照日期为数月前，断言被丢弃
    - 4.4: 回归 `daily_select` 快照路径只读 `amount`/`name`，不受本次改动影响

- [x] Task 5: 新增 `scripts/fill_kline_from_snapshot.py`
    - 5.1: 接受目标日期参数，走 `hub.get_market_snapshot` 而非裸解析端点
    - 5.2: 只写 `date == 目标日期` 的行，日期不符计数并抽样打印
    - 5.3: 日期不符占比 > 1% 判定快照非目标日定型值，整体中止不写库
    - 5.4: 停牌行（`close/volume/amount` 任一 ≤ 0）跳过；覆盖率 < 95% 不写库
    - 5.5: `pctChg` 用快照昨收计算，不依赖库内前一行

- [x] Task 6: 为补数脚本补测试
    - 6.1: mock 快照全为目标日期，断言写库行数与覆盖率日志
    - 6.2: mock 大面积日期不符，断言中止且未写库
    - 6.3: mock 覆盖率不足，断言中止且未写库
    - 6.4: mock 含停牌行，断言被跳过且不计入覆盖率分子

- [x] Task 7: 预筛加快照重试与历史日期放开
    - 7.1: 快照失败重试 3 次、退避 2/4 秒，每次打印尝试序号
    - 7.2: 放开历史日期——快照自带日期等于目标日即可用，不再要求 `end_date == today_str`
    - 7.3: 快照日期与目标日不符时不写缓存，直接进兜底

- [x] Task 8: 预筛 baostock 兜底加早停与空缓存防护
    - 8.1: 按失败率早停（样本下限 200、失败率阈值 0.9），复用同步侧既有语义
    - 8.2: 早停时打印已尝试数与失败率，与 `sync_kline_db` 的措辞保持一致
    - 8.3: 结果为空时删除缓存文件，不再落 1 字节空文件
    - 8.4: 清理 `cache/` 里已有的两个空 `_final.csv`

- [x] Task 9: 为预筛护栏补测试
    - 9.1: mock 快照前两次空、第三次有数据，断言重试生效且未进兜底
    - 9.2: mock 快照持续为空 + baostock 全失败，断言在样本下限附近退出而非跑满 5207
    - 9.3: 断言全失败后缓存文件不存在
    - 9.4: 回归缓存命中路径与覆盖率校验未被破坏

- [x] Task 10: 端到端验证与提交
    - 10.1: 用新脚本重跑一个已知日期（09-01），断言结果与现有库内数据逐字段一致
    - 10.2: 跑一次 `daily_select --date 2026-09-01`，确认命中缓存且候选数不变
    - 10.3: 刷新总览页，检查 `git status` 后提交
    - 10.4: 生成 summary.md，记录三条通路的耗时对比与快照日期语义变更
